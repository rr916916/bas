_schema-version: 3.3.0
ID: juno_invoice_assistant_v1
version: 1.0.0
description: "A simple CAP project."
parameters:
  enable-parallel-deployments: true
build-parameters:
  before-all:
    - builder: custom
      commands:
        - npm ci
        - npx cds build --production
modules:
  - name: juno_invoice_assistant_v1-srv
    type: nodejs
    path: gen/srv
    parameters:
      instances: 1
      buildpack: nodejs_buildpack
    build-parameters:
      builder: npm-ci
    provides:
      - name: srv-api # required by consumers of CAP services (e.g. approuter)
        properties:
          srv-url: ${default-url}
    requires:
      - name: juno_invoice_assistant_v1-auth
      - name: juno_invoice_assistant_v1-db
      - name: juno_invoice_assistant_v1-connectivity
      - name: juno_invoice_assistant_v1-destination
#      - name: juno_invoice_assistant_v1-logging

  - name: juno_invoice_assistant_v1-db-deployer
    type: hdb
    path: gen/db
    parameters:
      buildpack: nodejs_buildpack
    requires:
      - name: juno_invoice_assistant_v1-db

  - name: juno_invoice_assistant_v1
    type: approuter.nodejs
    path: app/router
    parameters:
      keep-existing-routes: true
      disk-quota: 256M
      memory: 256M
    requires:
      - name: srv-api
        group: destinations
        properties:
          name: srv-api # must be used in xs-app.json as well
          url: ~{srv-url}
          forwardAuthToken: true
      - name: juno_invoice_assistant_v1-auth
      - name: juno_invoice_assistant_v1-destination
#      - name: juno_invoice_assistant_v1-logging
    provides:
      - name: app-api
        properties:
          app-protocol: ${protocol}
          app-uri: ${default-uri}

resources:
  - name: juno_invoice_assistant_v1-auth
    type: org.cloudfoundry.managed-service
    parameters:
      service: xsuaa
      service-plan: application
      path: ./xs-security.json
      config:
        xsappname: juno_invoice_assistant_v1-${org}-${space}
        tenant-mode: dedicated
        oauth2-configuration:
          redirect-uris:
            - https://*~{app-api/app-uri}/**
    requires:
      - name: app-api
  - name: juno_invoice_assistant_v1-db
    type: com.sap.xs.hdi-container
    parameters:
      service: hana
      service-plan: hdi-shared
  - name: juno_invoice_assistant_v1-connectivity
    type: org.cloudfoundry.managed-service
    parameters:
      service: connectivity
      service-plan: lite
  - name: juno_invoice_assistant_v1-destination
    type: org.cloudfoundry.managed-service
    parameters:
      service: destination
      service-plan: lite
  # - name: juno_invoice_assistant_v1-logging
  #   type: org.cloudfoundry.managed-service
  #   parameters:
  #     service: application-logs
  #     service-plan: standard
